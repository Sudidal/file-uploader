<% locals.params.folders.forEach(folder => { %>
    <%- include(views.folderCard, {params: {folder: folder}}) %>
<% }) %> <% locals.params.files.forEach(file => { %>
    <%- include(views.fileCard, {params: {file: file}}) %>
<% }) %>
